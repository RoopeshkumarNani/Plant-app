# Supported Image Formats

## ✅ **Natively Supported Formats** (No Conversion Needed)

These formats work directly without any processing:

- **JPEG/JPG** - `.jpg`, `.jpeg` (most common)
- **PNG** - `.png` (recommended for quality)
- **BMP** - `.bmp` (Windows bitmap)
- **TIFF** - `.tiff`, `.tif` (high quality)
- **GIF** - `.gif` (animated or static)

## 🔄 **Auto-Converted Formats** (Will Be Converted to PNG)

These formats are automatically converted to PNG for processing:

- **WebP** - `.webp` (modern web format)
- **HEIC/HEIF** - `.heic`, `.heif` (iPhone photos)
- **SVG** - `.svg` (will be converted)
- **Any other image format** - Will attempt automatic conversion

## 📱 **Recommended For Different Devices**

### iPhone/iPad

- Original format: **HEIC** ✅ (auto-converted)
- Better option: Export as **PNG** or **JPEG**

### Android

- Original format: **JPEG** ✅ (native support)
- Alternative: **PNG** ✅ (native support)

### Desktop/Windows

- Best option: **PNG** or **JPEG** ✅
- Also supported: **BMP**, **TIFF**, **GIF** ✅

### Web Screenshots

- Format: **PNG** ✅ (native support)
- Format: **WebP** ✅ (auto-converted)

## 🎯 **Best Practices**

For optimal performance and fastest upload:

1. **Use JPEG** - Smaller file size, fast processing
2. **Use PNG** - Better quality, slightly larger
3. **Avoid WebP** - Requires conversion (slower)
4. **Avoid HEIC** - Requires conversion (slower)

## 💡 **How It Works**

When you upload an image:

1. ✅ **If supported format** → Used directly for processing
2. 🔄 **If unsupported format** → Automatically converted to PNG
3. 📊 **Image analysis** → Green area detected, species guessed
4. 🌿 **Database** → Original file format preserved, analysis saved

**You can upload ANY image format and it will work!** The app handles the conversion automatically.
